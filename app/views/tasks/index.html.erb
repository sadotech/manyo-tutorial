<!DOCTYPE html>
<html>
  <head>
    <title>タスク一覧 | Task Manage</title>
  </head>
  <body>
    <header>
      <% provide(:title, 'タスク一覧') %>
    </header>
    <div>
    <h1>タスク一覧</h1>
      <% @tasks.each do |task| %>
        <ul>
          <li><%= link_to task.content, task %></li>
        </ul>
      <% end %>
    </div>
  </body>
</html>